#include "main.c"

/**
 * _puts - a function that prints a string
 * @str: pointer str
 * Return: always 0
 */
void _puts(char *str)
{
	char c;
	int i;

	i = 0;
	while (*(s + i) != '\0')
	{
		printf("'%c'", *(s + i));
		i++;
	}

}
